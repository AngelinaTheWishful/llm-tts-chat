# LLM 角色扮演聊天 + TTS 开发工作流程

---

**文档版本**: WFv1.1
**最后更新**: 2026-07-31
**依据**: 项目开发需求书 RDv1.4
**状态**: 已确认（开发前生效）

---

## 一、开发范围硬约束

1. **所有开发工作严格限制在 `llm_tts_update` 文件夹内**，该文件夹外部禁止任何文件创建、编辑、删除操作
2. 禁止编辑、增添、删除任何 GPT-SoVITS 本体文件（包括 webui.py、api_v2.py、GPT_SoVITS/ 等）
3. 禁止修改 GPT-SoVITS 的 `requirements.txt` 及 `runtime/` 环境
4. 每个子文件夹内必须保留警示文件 `开发范围警示.md`，其标题和内容均为：`禁止在llm_tts_update以外位置增加，编辑，删除和其他开发工作。`；新增子文件夹时须同步创建该文件
5. 涉及 `temp_audio/` 的自动清理逻辑必须跳过 `开发范围警示.md`

---

## 二、版本管理总览

本项目采用**三套独立版本号**，互不干扰：

| 版本体系 | 格式 | 位置 | 用途 |
|----------|------|------|------|
| 需求文档版本 | `RDv<主>.<次>` | 项目开发需求书.md 头部 | 追踪需求变更 |
| 应用版本 | `v<主>.<次>.<修订>` (SemVer) | Git tag + CHANGELOG.md | 发布追踪 |
| 数据版本 | `"<主>.<次>"` (如 "1.0") | config.json `data_version` 字段 | 数据迁移判断 |

### 2.1 需求文档版本（RDv）

- 每次需求变更（新增/修改/删除章节）递增
- 主版本：结构性重写；次版本：新增需求或确认轮次
- 变更时必须同步：头部版本号 + 文件末尾变更历史表 + git commit
- 当前：RDv1.2

### 2.2 应用版本（SemVer）

```
v<主版本>.<次版本>.<修订>

主版本: 不兼容的架构变更或重大重写
次版本: 新功能或重大改进（对应里程碑）
修订:   bug 修复、小优化、文档更新

示例:
v1.0.0 - 初始版本（基础聊天 + TTS）
v1.1.0 - 角色编辑功能完成
v1.1.1 - 修复音频上传 bug
v1.2.0 - 多语言支持完成
v2.0.0 - 架构重构
```

- 版本 tag 打在 main 分支合并点
- 每次发布必须同步更新 CHANGELOG.md

### 2.3 数据版本（data_version）

- 存储于 config.json 顶层 `data_version` 字段
- 应用启动时检测：`check_data_version()` 比对当前版本
- 数据格式变更时必须递增数据版本，并编写对应迁移脚本（`migrations/vX.Y_to_vX.Y.py`）
- 迁移流程：备份 → 迁移 → 更新版本 → 失败回滚

---

## 三、Git 版本管理

### 3.1 仓库位置与分支策略

```
仓库根目录: llm_tts_update\.git
仅管理本项目文件，不包含 GPT-SoVITS 本体

main 分支（稳定）
  ├─ 仅用于稳定发布版本
  ├─ 只从 dev 合并，禁止直接提交
  ├─ 合并前必须通过测试验证
  └─ 合并后打版本 tag

dev 分支（日常开发）
  ├─ 日常开发主分支
  ├─ 保持可运行状态（不提交半成品导致崩溃）
  └─ 准备发布时合并到 main
```

### 3.2 分支保护规则

- pre-commit hook 强制禁止向 main 直接提交
- 每次 commit 自动执行 ruff 检查
- 重大功能开发建议使用 `feature/*` 分支，完成后合并回 dev

### 3.3 Commit 规范（Conventional Commits）

```
<类型>: <英文标题简述>

<中文详细描述（可选）>

<关联需求章节（可选）>
```

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat: 添加角色头像上传功能` |
| `fix` | 修复 bug | `fix: 修复音频文件路径包含空格时转换失败` |
| `docs` | 文档更新 | `docs: 更新 README 安装步骤` |
| `style` | 代码格式调整 | `style: 修复 ruff 格式警告` |
| `refactor` | 重构 | `refactor: 提取 TTSClient 基类` |
| `perf` | 性能优化 | `perf: 优化 Lorebook 匹配速度` |
| `test` | 测试相关 | `test: 添加 LLMClient 单元测试` |
| `chore` | 构建/工具/配置 | `chore: 添加 .gitignore 配置` |

### 3.4 版本控制常用流程

```bash
# 初始化（仅一次）
git init
git checkout -b main
git add README.md .gitignore config.example.json pyproject.toml
git commit -m "chore: 初始化项目结构"
git checkout -b dev

# 日常开发（在 dev 上）
git add .
git commit -m "feat: 添加角色编辑功能

实现了 WebUI 表单编辑角色配置的功能，包括：
- 基本信息、性格、背景、口癖等字段编辑
- 头像上传与裁切
- Lorebook 条目编辑

关联需求：项目开发需求书.md - 二十八、角色编辑方式"

# 准备发布
git checkout main
git merge dev
git tag v1.1.0
git push origin main --tags
```

---

## 四、单功能开发流程（标准迭代）

每个功能的开发遵循以下固定流程：

```
1. 需求确认
   └─ 对照需求书确认功能范围与验收标准
       │
2. 技术方案
   └─ 若涉及未确认选型 → 与需求方确认后更新需求书（RDv 递增）
       │
3. 编码实现
   └─ 遵循代码规范（章节七），按模块设计实现
       │
4. 自测
   ├─ 单元测试（pytest）
   ├─ ruff check + ruff format
   └─ 手动功能验证（UI 操作）
       │
5. 提交
   ├─ Conventional Commits 规范
   ├─ 仅提交本功能相关文件
   └─ pre-commit 自动检查（ruff + main 分支保护）
       │
6. 合并与同步
   └─ feature/* → dev（若使用功能分支）
       │
7. 记录
   └─ 更新开发工作记录文件
```

---

## 五、开发阶段规划（Phase 1-8）

| 阶段 | 内容 | 完成标准 | 里程碑版本 |
|------|------|----------|-----------|
| Phase 1 | 项目骨架：目录结构 + config.json + 基类 + 配置向导 | 应用可启动，首次启动进入向导 | — |
| Phase 2 | TTSClient + LLMClient（含重试/串行化/分片） | 单元测试通过，真实调用成功 | v1.0.0 |
| Phase 3 | CharManager + ConvManager（含摘要压缩/Lorebook） | 单元测试通过 | v1.0.0 |
| Phase 4 | app.py Gradio UI（左右分栏 + 对话流程 + 音频） | 端到端对话可用（文字+语音） | v1.0.0 |
| Phase 5 | 角色编辑、Lorebook 编辑、多语言、主题 | 功能验收通过 | v1.1.0 |
| Phase 6 | 导入导出（角色/会话/收藏）、搜索、统计 | 功能验收通过 | v1.2.0 |
| Phase 7 | 测试补全 + CI + 文档（README/Wiki） | CI 绿、覆盖率达标 | v1.3.0 |
| Phase 8 | 打包发布（zip + NSIS 安装器） | 发布流程可复现 | v2.0.0 |

### 阶段内子任务规范

- 每个阶段拆分为多个可独立提交的子任务
- 每个子任务完成后保持 dev 可运行
- 阶段里程碑达成 → 创建基线（见章节八）

---

## 六、代码规范与质量

### 6.1 ruff 配置（pyproject.toml）

```toml
[tool.ruff]
target-version = "py39"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP"]
```

### 6.2 检查命令

```bash
venv\Scripts\pip install ruff
venv\Scripts\ruff check modules/ app.py
venv\Scripts\ruff format modules/ app.py
```

### 6.3 编码约定

| 规则 | 要求 |
|------|------|
| 缩进 | 4 空格，不使用 Tab |
| 行宽 | 最大 100 字符 |
| 引号 | 优先使用双引号 |
| 命名 | 类 `PascalCase`，函数/变量 `snake_case`，常量 `UPPER_CASE` |
| 类型标注 | 函数参数和返回值标注类型（推荐，不强制） |
| import 顺序 | 标准库 → 第三方 → 本地，每组空行分隔 |
| 文件编码 | UTF-8 |

---

## 七、测试流程

### 7.1 测试层级

```
┌─ E2E 集成测试 ─────┐  ← 模拟完整用户操作流程（手动 + 自动）
├─ 集成测试 ─────────┤  ← 模块间协作测试（LLM+TTS 联调）
├─ 单元测试 ─────────┤  ← 各模块独立测试 (pytest)
└─ 代码规范检查 ─────┘  ← ruff + 类型检查
```

### 7.2 测试目录

```
llm_tts_update/
├── tests/
│   ├── __init__.py
│   ├── test_llm_client.py
│   ├── test_tts_client.py          # mock API
│   ├── test_character_manager.py
│   ├── test_conversation_manager.py
│   ├── test_lorebook_matcher.py
│   └── test_integration.py
├── pytest.ini
└── pyproject.toml
```

### 7.3 测试运行要求

- 提交前：涉及模块的单元测试必须通过
- 基线前：全部测试通过 + ruff 无错误
- CI（GitHub Actions）：push 到 main/dev 自动运行 ruff + pytest

---

## 八、基线（Baseline）管理

### 8.1 基线创建条件（任一满足）

- **里程碑达成**：重大功能完成并通过测试
- **定期基线**：每月 1 号
- **紧急修复**：严重 bug 修复后

### 8.2 基线流程

```
1. dev 功能开发完成
2. 本地测试验证（手动 + 自动）
3. 合并到 main
4. 打 tag: v1.2.0
5. 推送 tag: git push origin v1.2.0
6. 更新 CHANGELOG.md
7. 创建 GitHub Release（可选）
```

### 8.3 基线质量门禁

| 检查项 | 要求 |
|--------|------|
| 单元测试 | 100% 通过 |
| ruff check | 0 错误 |
| 手动功能验证 | 核心流程（聊天+TTS）无阻塞 bug |
| 数据迁移 | 从上一基线数据迁移成功 |
| CHANGELOG | 已记录本版本变更 |

---

## 九、发布流程

### 9.1 发布渠道

| 渠道 | 内容 | 顺序 |
|------|------|------|
| GitHub 源码 | 源码 + 文档 + tag | 1 |
| 预打包 zip | 源码 + venv + 启动脚本 | 2 |
| NSIS 安装器 | 一键安装 + 桌面快捷方式 | 3 |

### 9.2 发布检查清单

```
□ 版本号已确定（SemVer）
□ CHANGELOG.md 已更新
□ README.md 截图/说明已更新
□ 全部测试通过
□ main 分支 = dev 最新稳定版
□ tag 已推送
□ GitHub Release 已创建
□ 数据迁移脚本已验证
□ 已执行脱敏发布流程（见章节十三）且扫描零残留
□ 一次性 PAT 令牌已删除
```

---

## 十、更新与备份流程

### 10.1 应用更新（update.bat）

```
1. 备份用户数据（characters/ conversations/ config.json/ logs/ trash/）
2. 下载新版本
3. 替换可替换文件（app.py modules/ locales/ requirements.txt 等）
4. 保留用户数据目录（不清除）
5. 完成
```

### 10.2 WebUI 一键备份

- 设置 Tab → "导出所有数据" → zip（角色 + 会话 + 配置）
- 建议每次大版本更新前执行

### 10.3 数据迁移流程

```
启动时检测 data_version
  ├─ 版本一致 → 正常启动
  ├─ 版本不一致 → 自动备份旧数据 → 执行迁移脚本 → 更新版本号
  │     └─ 迁移失败 → 从 backup/ 恢复 → UI 提示
```

---

## 十一、文档维护规范

### 11.1 文档层级

| 文档 | 更新时机 |
|------|----------|
| 项目开发需求书.md | 需求变更时（RDv 递增） |
| 开发工作流程.md | 流程/规范变更时（WFv 递增） |
| 【日期】开发工作记录 | 每次开发任务完成后 |
| CHANGELOG.md | 每次版本发布时 |
| README.md | 功能/截图变化时 |
| GitHub Wiki | 基线发布时 |

### 11.2 文档变更流程

```
1. 提出变更（需求/流程）
2. 需求方确认
3. 更新对应文档 + 版本号递增
4. 文档内变更历史表追加新行
5. git commit -m "docs: 更新...（版本号递增）"
```

---

## 十二、开发范围警示（硬性纪律）

- 开发全过程中任何时刻，**禁止在任何 `llm_tts_update` 以外的位置创建、编辑、删除文件**
- 若工具/脚本需要临时文件，一律放在 `{系统临时目录}`（已预授权）
- 新增任何子文件夹时，必须同步放置 `开发范围警示.md`
- 违反此规则的操作必须立即回滚并记录到开发工作记录

---

## 十三、GitHub 发布流程（脱敏）

### 13.1 基本原则

- 本地仓库（`llm_tts_update\.git`）包含完整敏感文档（绝对路径、需求书等），**不与 GitHub 共享**
- GitHub 仓库使用**脱敏副本**发布，两个仓库完全独立
- 每次发布新版本都必须重新执行脱敏流程

### 13.2 敏感数据清单

| 数据 | 示例 | 处理 |
|------|------|------|
| 绝对路径 | `llm_tts_update` | → `llm_tts_update` |
| GPT-SoVITS 根目录 | `C:\...\GPT-SoVITS-v2pro-20250604` | → `D:/GPT-SoVITS/...` 占位 |
| 系统临时目录 | `{系统临时目录}` | → `{系统临时目录}` |
| 内部文档 | 项目开发需求书.md、开发工作记录 | 不发布 |
| 提交作者 | {GitHub 用户名} / 真实邮箱 | 用中性身份 `llm-tts-chat` |
| API Key / 令牌 | `ghp_xxx`、`sk-xxx` | 绝不入仓库，推送后清理配置 |

### 13.3 发布流程

```
1. 构建脱敏副本（临时目录）
   └─ 复制全部文件，排除 .git/ venv/ 需求书/ 工作记录
       │
2. 脱敏替换
   ├─ 绝对路径 → 通用占位（见 13.2）
   ├─ 警示文件路径同步脱敏
   └─ 发布版 .gitignore 追加排除内部文档
       │
3. 扫描验证
   └─ 正则扫描副本：本地绝对路径 / 用户名 / 邮箱 / 令牌 → 确认零残留
       │
4. 初始化发布仓库（中性身份）
   ├─ git config user.name "llm-tts-chat"
   ├─ git config user.email "llm-tts-chat@users.noreply.github.com"
   └─ 分支 main + dev，提交后推送
       │
5. 推送与清理
   ├─ 临时用 PAT 令牌推送（x-access-token）
   └─ 推送后立即：git remote set-url 移除令牌 + 修正 upstream
       │
6. 提示用户删除 PAT 令牌
```

### 13.4 发布后安全确认

- 通过 raw.githubusercontent.com 无鉴权抽查公开内容，确认无敏感数据
- 提醒用户删除一次性 PAT 令牌
- 更新开发工作记录（发布仓库提交号、URL、脱敏记录）

### 13.5 快速命令参考

```bash
# 推送（临时令牌，推送后清理）
git push https://x-access-token:<PAT>@github.com/<用户>/llm-tts-chat.git main
git remote set-url origin https://github.com/<用户>/llm-tts-chat.git
git config branch.dev.remote origin
```

---

## 变更历史

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| WFv1.0 | 2026-07-31 | 初始版，基于需求书 RDv1.2 编写 | system |
| WFv1.1 | 2026-07-31 | 新增章节十三：GitHub 脱敏发布流程；发布检查清单补充脱敏项 | system |
