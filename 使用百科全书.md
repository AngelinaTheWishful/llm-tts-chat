# LLM 角色扮演聊天 + GPT-SoVITS TTS — 使用百科全书

> 面向从零开始接触电脑的用户，从下载安装到使用全部功能的一站式指南。
> 无论你从未用过命令行，还是第一次接触"AI 聊天"和"语音合成"，跟随本指南逐步操作即可。

---

## 目录

1. [开始之前：你需要知道的基础概念](#一开始之前你需要知道的基础概念)
2. [第一步：认识你的电脑（文件夹、路径、压缩包）](#二第一步认识你的电脑文件夹路径压缩包)
3. [第二步：下载并安装 GPT-SoVITS v2](#三第二步下载并安装-gpt-sovits-v2)
4. [第三步：获取本项目（克隆或下载）](#四第三步获取本项目克隆或下载)
5. [第四步：认识项目文件夹](#五第四步认识项目文件夹)
6. [第五步：安装本项目依赖](#六第五步安装本项目依赖)
7. [第六步：首次启动与配置向导](#七第六步首次启动与配置向导)
8. [第七步：开始你的第一次对话](#八第七步开始你的第一次对话)
9. [功能百科全书（逐项详解）](#九功能百科全书逐项详解)
10. [常见问题 FAQ](#十常见问题-faq)
11. [术语表](#十一术语表)
12. [安全注意事项](#十二安全注意事项)

---

## 一、开始之前：你需要知道的基础概念

### 1.1 这是什么项目？

这是一个**角色扮演聊天 + 语音合成**程序，由三部分协作完成：

| 部分 | 作用 | 类比 |
|------|------|------|
| **LLM（大语言模型）** | 负责"思考和说话"——理解你的输入并组织回复文字 | 一个聪明的"大脑" |
| **GPT-SoVITS** | 负责"发声"——把文字变成有感情、有角色的声音 | 一个"声优" |
| **本程序（Gradio 界面）** | 把上面两者连接起来，给你一个可视化的聊天窗口 | 一个"遥控器/控制台" |

**你做的事**：在网页聊天框里打字，选择一个角色（比如"温柔学姐"），AI 用这个角色的人设回复你，并用这个角色的声音把回复念出来。

### 1.2 三个关键概念

- **LLM（大语言模型）**：一种 AI 程序，能理解文字并生成文字。本项目使用"OpenAI 兼容 API"接入，常见提供商有 DeepSeek、OpenAI、通义千问等。你需要一个 API Key（相当于"门票"）才能使用。
- **TTS（Text-To-Speech，文字转语音）**：把文字转换成语音的技术。本项目使用 GPT-SoVITS，可以克隆特定角色的音色。
- **API（应用程序接口）**：程序之间互相通信的"接头暗号"和"联络方式"。GPT-SoVITS 启动后会提供一个 API 服务地址，本程序通过它调用语音合成。

### 1.3 你需要准备什么？

- 一台 **Windows 10 或 11** 的电脑
- 一定的**硬盘空间**（GPT-SoVITS 模型较大，建议预留 10GB 以上）
- 一个**网络连接**（下载资源、调用 LLM 需要）
- 一个 **LLM API Key**（如 DeepSeek 等，后面会详细说明如何获取）

---

## 二、第一步：认识你的电脑（文件夹、路径、压缩包）

### 2.1 文件夹（目录）

电脑里的文件存放在"文件夹"中。文件夹里可以再放文件夹，形成层级结构。

```
D:\
├── 我的项目\          ← 一个文件夹
│   ├── 工具\          ← 文件夹里的文件夹
│   └── 说明.txt        ← 文件
└── 游戏\
```

### 2.2 路径（地址）

每个文件/文件夹都有一个"地址"，叫**路径**。例如：

- `D:\GPT-SoVITS` —— 表示 D 盘里叫 GPT-SoVITS 的文件夹
- `D:\GPT-SoVITS\runtime\python.exe` —— 表示该文件夹下 runtime 文件夹里的 python.exe 文件
- 路径中的 `\` 是分隔符（Windows 系统）

**小技巧**：在"文件资源管理器"顶部的地址栏输入路径，可以直接跳到那个位置。

### 2.3 压缩包（zip / rar / 7z）

网上下载的大文件通常是"压缩包"，像行李箱一样把很多文件打包成一个。你需要先"解压"才能使用。

- **右键压缩包** → 选择"解压到当前文件夹"或"解压到 xxx"即可
- 建议使用系统自带的解压功能，或安装 7-Zip / WinRAR

### 2.4 文件资源管理器（打开文件夹的地方）

- 按键盘 `Win` 键 + `E`，即可打开"文件资源管理器"
- 它就是管理你电脑文件的"窗户"

### 2.5 命令行/终端（黑色的输入窗口）

有些操作需要在"命令行"里输入文字。别害怕，只需要学会"复制粘贴"即可。

打开方法：在文件夹的地址栏输入 `cmd` 然后回车，就会在**当前文件夹**打开一个黑色窗口。

**记住一个原则**：所有需要你输入的代码，都可以直接**复制**后，在黑色窗口里**右键粘贴**，然后按回车。

---

## 三、第二步：下载并安装 GPT-SoVITS v2

GPT-SoVITS 是本项目的"声优"，必须先行安装。

### 3.1 下载 GPT-SoVITS

1. 打开浏览器，访问 GPT-SoVITS 官方项目页面（GitHub 上的 `RVC-Boss/GPT-SoVITS`）
2. 找到 **Releases（发行版）** 页面
3. 下载与 v2 / v2Pro 对应的压缩包（文件名通常类似 `GPT-SoVITS-xxx.7z` 或 `.zip`）
4. 本项目适配 **v2 / v2Pro** 版本

> 提示：如果你是通过其他渠道获得的本项目配套资源包（通常附带 GPT-SoVITS 本体），直接使用其中已解压好的 GPT-SoVITS 文件夹即可，跳过第 2、3 步。

### 3.2 解压 GPT-SoVITS

1. 在硬盘上建一个存放位置，例如 `D:\GPT-SoVITS`
2. 把下载的压缩包放进去
3. **右键压缩包** → 解压到当前文件夹
4. 解压后你会看到一个类似 `GPT-SoVITS-v2pro-20250604` 的文件夹

**请记住这个文件夹的完整路径**（例如 `D:\GPT-SoVITS\GPT-SoVITS-v2pro-20250604`），后面配置时会用到。你可以通过"文件资源管理器"地址栏复制它。

### 3.3 确认关键文件存在

进入解压后的文件夹，确认有以下内容：

- `api_v2.py`（文件）
- `runtime`（文件夹，里面有一个 python.exe）

如果缺少 `api_v2.py`，说明版本不对，请重新下载 v2 版本。

### 3.4 启动 GPT-SoVITS 语音服务

> **v1.2.0 起推荐直接用「一键启动」（见 5.4 节）：`go-llm-tts.bat` 会自动拉起 GPT-SoVITS 语音服务与聊天应用**，无需手动执行本节。以下为手动方式（备用）。

本程序需要 GPT-SoVITS 的 **API 服务**持续运行。启动方法有两种：

**方法 A：使用命令行（推荐，稳定）**

1. 进入 GPT-SoVITS 解压后的文件夹（例如 `D:\GPT-SoVITS\GPT-SoVITS-v2pro-20250604`）
2. 在地址栏输入 `cmd` 回车，打开命令行
3. 输入以下命令并回车：

```
runtime\python.exe api_v2.py -a 127.0.0.1 -p 9880
```

4. 看到类似 `Uvicorn running on http://127.0.0.1:9880` 的信息，说明服务启动成功
5. **这个黑色窗口不要关闭**，保持它在后台运行

**方法 B：使用配套的启动脚本**

如果你下载的 GPT-SoVITS 自带 `go-webui.bat` 之类的脚本，在网页界面中找到 API 相关按钮启动即可（不同版本界面不同，以方法 A 为准）。

### 3.5 验证服务是否正常运行

在浏览器地址栏输入：

```
http://127.0.0.1:9880/control
```

如果看到类似 `{"status":"ok"}` 的文字，说明语音服务已就绪。

> `127.0.0.1:9880` 表示"本机的 9880 端口"，这是 GPT-SoVITS 语音服务的默认地址，本项目默认使用它。

---

## 四、第三步：获取本项目（克隆或下载）

本项目名称是 `llm-tts-chat`，代码托管在 GitHub 上。

### 4.1 方式 A：使用 Git 克隆（推荐，便于更新）

如果你电脑上已经安装了 **Git**：

1. 在硬盘上建一个项目目录，例如 `D:\LLM项目`
2. 进入该文件夹，地址栏输入 `cmd` 回车
3. 输入以下命令并回车：

```
git clone https://github.com/AngelinatheMellowWish/llm-tts-chat.git
```

4. 完成后，`D:\LLM项目` 下会出现一个 `llm-tts-chat` 文件夹，这就是本项目

**小提示**：Git 下载地址 https://git-scm.com/download/win ，安装时一路"下一步"即可。

### 4.2 方式 B：直接下载压缩包（无需 Git）

1. 打开项目网页 `https://github.com/AngelinatheMellowWish/llm-tts-chat`
2. 点击绿色的 **Code** 按钮 → **Download ZIP**
3. 下载后**解压**，得到一个 `llm-tts-chat-main` 文件夹
4. **强烈建议**把文件夹改名为 `llm-tts-chat`（避免名字里带 `-main`），并放到一个稳定位置，例如 `D:\LLM项目\llm-tts-chat`

> 无论用哪种方式，请把项目的**完整路径**记下来，例如 `D:\LLM项目\llm-tts-chat`。

---

## 五、第四步：认识项目文件夹

进入项目文件夹，你会看到这些内容：

```
llm-tts-chat/
├── app.py                  ← 主程序（入口）
├── go-llm-tts.bat          ← 一键启动脚本
├── install_deps.bat        ← 一键安装依赖脚本
├── build_zip.bat           ← 打包脚本
├── requirements.txt        ← 依赖清单
├── config.example.json     ← 配置示例
├── modules/                ← 核心代码
├── characters/             ← 角色文件夹（每个角色一个子文件夹）
├── conversations/          ← 对话记录（每个会话一个子文件夹）
├── locales/                ← 多语言文件
├── migrations/             ← 数据迁移脚本
├── tests/                  ← 测试代码
├── .github/                ← CI 配置
└── README.md               ← 项目说明
```

**你平时需要关心的主要是**：
- `go-llm-tts.bat` —— 双击启动程序
- `install_deps.bat` —— 首次使用前安装依赖
- `characters/` —— 想手动放角色就放这里
- `config.json` —— 配置文件（首次配置向导会自动生成）

---

## 六、第五步：安装本项目依赖

本程序需要安装一些"零件"（专业术语叫依赖），这一步只需要做一次。

### 6.1 方法：双击脚本

1. 进入项目文件夹
2. **双击** `install_deps.bat`
3. 会出现一个黑色窗口，自动开始安装
4. 看到 `[INFO] 完成` 并且窗口暂停，说明安装成功
5. 按任意键关闭窗口

### 6.2 安装了什么？

- 创建了一个独立的虚拟环境 `venv`（相当于给本项目单独隔离的"沙箱"，不污染你电脑上的其他软件）
- 安装了 `gradio`（网页界面）、`openai`（连接 LLM）、`jieba`（中文分词）等依赖

> 如果安装失败，多半是网络问题，重试几次即可；或咨询网络代理设置。

---

## 七、第六步：首次启动与配置向导

### 7.1 启动项目

**v1.2.0 起为一键启动**：项目文件夹里的 `go-llm-tts.bat` 会自动完成两件事：

1. 自动探测与项目**同一父目录**下的 `GPT-SoVITS*` 文件夹，若 TTS API（9880）未在运行，则**自动开启一个窗口**启动语音服务（api_v2.py，模型加载约 30~90 秒）
2. 语音服务就绪后，**再开一个窗口**启动聊天应用，并自动打开浏览器进入网页界面

操作只需：

1. **双击**项目文件夹里的 `go-llm-tts.bat`
2. 稍等片刻：出现"GPT-SoVITS TTS API"窗口（若 TTS 未在运行）→ 出现"LLM 角色扮演聊天"窗口 → 浏览器自动打开
3. 如果浏览器没有自动打开，可以手动访问 `http://127.0.0.1:7861`

> 记住：这两个黑色窗口是程序的"心脏"，**不要关闭**。关闭窗口 = 关闭对应程序。
>
> 若 TTS 窗口启动失败/超时，聊天应用仍会打开（此时 TTS 显示离线、无语音），可查看 `logs\startup_report_*.txt`（含 STP 错误码）定位原因。

### 7.2 首次启动配置向导

第一次启动时，网页会显示一个**配置向导**，分三步填写：

**步骤 1：GPT-SoVITS 配置**

| 填写项 | 填什么 |
|--------|--------|
| GPT-SoVITS 本体路径 | 你在[第三步](#三第二步下载并安装-gpt-sovits-v2)记住的那个路径，如 `D:\GPT-SoVITS\GPT-SoVITS-v2pro-20250604` |
| TTS API 地址 | 保持默认 `http://127.0.0.1:9880` |

**步骤 2：LLM 配置**（这是"大脑"的配置）

| 填写项 | 说明 |
|--------|------|
| 提供商名称 | 给这个提供商起个名字，如 `deepseek` |
| API Base URL | 提供商提供的接口地址，如 DeepSeek 为 `https://api.deepseek.com/v1` |
| API Key | 你的密钥（后面[8.2](#82-获取-llm-api-key)说明如何获取） |
| 模型名称 | 如 `deepseek-chat` |
| Max Tokens | 回复最大长度，保持 2048 即可 |
| Temperature | 创意度，越高越天马行空，0.8 较均衡 |
| 回复文字语种 | 让 AI 用什么语言回复你 |

**步骤 3：初始角色选择**：选择或跳过（可稍后选）

填好后点击 **完成配置**，进入聊天主界面。

### 7.3 如何获取 LLM API Key？

以 DeepSeek 为例：

1. 访问 DeepSeek 官网（platform.deepseek.com），注册账号
2. 进入"API Keys"页面，点击"创建 API Key"
3. 复制生成的 Key（形如 `sk-xxxxxxxx`）
4. 在配置向导中粘贴

> **安全提醒**：API Key 相当于你的"钱包密码"，会按使用量扣费。**千万不要把它发给别人**，不要截图分享。本项目会以 base64 编码后存入本机 `config.json`，请勿分享该文件。
>
> **小提示**：粘贴 API Key 时请勿带上开头/结尾的空格或换行（应用会自动清理，但避免为好）。

---

## 八、第七步：开始你的第一次对话

### 8.1 界面布局

进入主界面后，你会看到：

```
┌──────────────────────────┬──────────────────────────────┐
│ 左栏（控制区）            │ 右栏（聊天区）               │
│  界面语言 / 主题          │                              │
│  ┌─ 角色 ──────────────┐ │  ┌─ 聊天框 ────────────────┐ │
│  │ [选择角色 ▼]        │ │  │  AI 的回复显示在这里      │ │
│  │ [🔄 刷新角色]       │ │  └──────────────────────────┘ │
│  │ [编辑角色]          │ │  语音回复播放器              │ │
│  ├─ 会话 ──────────────┤ │  ┌─ 输入区 ────────────────┐ │
│  │ [➕ 新建会话]        │ │  │ 输入你的话...            │ │
│  │ 会话列表            │ │  └──────────────────────────┘ │
│  ├─ 状态 ──────────────┤ │  [⭐ 收藏] [发送]             │ │
│  │ 服务运行状态        │ │                              │ │
│  ├─ 工具 ──────────────┤ │                              │ │
│  │ 导出/导入/搜索/统计  │ │                              │ │
│  └─────────────────────┘ └──────────────────────────────┘
│ 状态栏: TTS 在线 | 提供商 | 会话                          │
└──────────────────────────────────────────────────────────┘
```

### 8.2 开始对话

1. **选择角色**：在左栏"角色"下拉框选择一个角色（首次使用可能为空，需要先[导入或创建角色](#95-角色编辑)）
2. **新建会话**：点击"新建会话"，AI 会自动说一句问候语（有语音）
3. **输入并发送**：在底部输入框打字，按 **Enter** 发送；**Shift + Enter** 换行
4. **等待回复**：AI 生成文字 → 自动合成语音 → 显示在聊天框，语音自动播放

> 状态栏显示 `🟢 TTS API 在线` 说明语音服务正常。如果显示红色 `🔴 TTS API 离线`，说明 GPT-SoVITS 服务没启动，请回到[第 3.4 步](#34-启动-gpt-sovits-语音服务)。

---

## 九、功能百科全书（逐项详解）

### 9.1 角色系统

**角色是什么？** 角色是一套"人设"，包括名字、性格、说话风格、背景故事、口癖、思维链、问候语、音色等。每个角色是一个文件夹：

```
characters/
└── 温柔学姐/                  ← 角色文件夹（名字 = 文件夹名）
    ├── character.json         ← 角色全部文字配置
    ├── portrait.png           ← 头像（正面上半身）
    ├── portrait_thumb.png     ← 头像缩略图（自动生成）
    ├── ref.wav                ← 参考音频（音色样本 3~10 秒）
    ├── greeting.wav           ← 问候语语音（可选）
    ├── voice_sample.wav       ← 音色试听（可选）
    └── lorebook.json          ← 世界观知识库（可选）
```

**切换角色**：左栏"角色"下拉框选择，会自动应用该角色预设的音色模型。

**角色预设**：`character.json` 里的 `recommended_settings` 记录了该角色推荐的 GPT 模型、SoVITS 模型、参考音频，切换角色时自动加载。

### 9.2 会话管理

**会话是什么？** 一次连续的聊天记录。每次对话保存在一个会话里。

| 操作 | 方法 |
|------|------|
| 新建会话 | 点击"新建会话"，自动生成角色问候语 |
| 切换会话 | 点击左栏"会话列表"中的一项 |
| 导出会话 | 左栏"工具"→"导出会话"，下载为 zip |
| 导入会话 | 左栏"工具"→"导入会话"，选择 zip 文件 |
| 删除/重命名 | 会话自动命名"角色名-序号"，可在文件管理器中删除 `conversations/` 下的对应文件夹 |

**会话存储**：每个会话一个文件夹在 `conversations/` 下：
```
conversations/
└── sess_20260731_102030_ab12/
    ├── messages.json          ← 聊天记录
    ├── summary.txt            ← 历史摘要
    ├── name.txt               ← 会话名称
    └── audio/                 ← 语音文件
        ├── msg_0.wav
        └── msg_1.wav
```

**长对话自动压缩**：当一个会话轮次过多（默认 20 轮），程序会把早期对话压缩成一段摘要，只保留最近几轮，保证 AI 不会"忘记"太多且不超长。

### 9.3 发送与接收

- **输入快捷键**：`Enter` 发送；`Shift+Enter` 换行；`Ctrl+Enter` 也发送
- **回复显示**：AI 回复支持 Markdown 排版（标题、加粗、列表、代码块等）
- **语音**：每条 AI 回复会自动合成语音并自动播放；可在播放器手动重播
- **超长回复**：超过 800 字的回复会自动分片合成语音再拼接，避免语音中断

### 9.4 聊天设置

在右栏顶部有两个下拉框：

| 设置 | 作用 |
|------|------|
| **回复文字语种** | AI 用什么语言回复（中文/日本語/English） |
| **合成语音语种** | 语音用什么语言模式合成 |

这两个可以分开设置。例如：让 AI 用日文回复，但语音用中文模式合成（GPT-SoVITS 会尽力用中文发音规则念日文）。

### 9.5 角色编辑

左栏"编辑角色"面板，可以编辑当前角色的全部文字配置：

| 区域 | 内容 |
|------|------|
| 角色名称 | 角色叫什么 |
| 问候语 | 开场白 |
| 性格 | 性格描述 |
| 说话风格 | 说话方式 |
| 口癖（每行一条） | 口头禅、习惯用词，如"句尾喜欢加'呢'" |
| 背景故事 | 角色的身世背景 |
| 喜好/厌恶（每行一条） | 喜欢/讨厌的事物 |
| 行为准则（每行一条） | 角色的行为底线 |
| 思维链 (CoT) | 回复前的思考步骤 |
| Lorebook | 世界观知识库（见 9.6） |
| 上传头像 | 上传角色头像（自动裁切为 1:1） |

编辑完成后点击 **保存角色**。保存后新消息使用新配置，已完成的对话保留旧配置。

### 9.6 Lorebook 世界观知识库

Lorebook 是角色的"背景知识库"。当用户输入提到相关知识时，AI 会自动"回忆"起来。

**格式**：每行一条，格式为 `关键词1,关键词2:内容`

```
弟弟,弟弟的情况:学姐有个上初中的弟弟叫小杰，很懂事
钢琴,弹琴,音乐:学姐从小练钢琴，考过八级，最爱《致爱丽丝》
```

**匹配原理**：程序用中文分词（jieba）把用户输入拆成词语，与关键词匹配（含同义词扩展）。匹配到的条目会自动附加到 AI 的提示词中，让回复更贴合角色设定。

### 9.7 收藏消息

- 点击 **⭐ 收藏最后一条回复**：把最后一条 AI 回复加入收藏
- 再次点击：取消收藏
- 收藏数据保存在 `conversations/{会话}/favorites.json`

### 9.8 搜索

左栏"工具"→"搜索"：

- 在**当前会话**内搜索关键词
- 结果按时间显示，包含上下文

### 9.9 统计

左栏"工具"→"查看统计"：

- **本会话统计**：消息数、用户/AI 消息数、收藏数、语音数
- **全局统计**：所有会话数、总消息数、收藏总数、语音总数、最活跃会话

### 9.10 多语言界面

顶部"界面语言"下拉框可切换：

- **中文**（默认）
- **日本語**
- **English**

切换后页面自动刷新并应用新语言。

### 9.11 主题

顶部"主题"下拉框可切换：

- **浅色**
- **深色**

切换后页面自动刷新。如需更精细的自定义（颜色、气泡样式、自定义 CSS），可在项目文件夹的 `theme_config.json` 中配置（参照 `modules/theme.py` 中 LIGHT_THEME/DARK_THEME 的结构）。

### 9.12 语音设置（config.json）

项目的详细设置保存在项目根目录的 `config.json`（首次配置向导自动生成）。常用字段：

```json
{
  "tts": {
    "api_base_url": "http://127.0.0.1:9880",
    "voice_language": "中文",
    "top_k": 15,
    "top_p": 1.0,
    "temperature": 1.0,
    "speed": 1.0
  },
  "audio_normalization": {
    "enabled": true,
    "target_dB": -3.0,
    "global_volume": 1.0
  },
  "prompt_protection": {
    "mode": "A"
  }
}
```

| 字段 | 作用 |
|------|------|
| `tts.top_k / top_p / temperature / speed` | 语音合成参数（一般保持默认） |
| `audio_normalization` | 音量标准化（目标 -3dB，避免太响或太轻） |
| `prompt_protection.mode` | 提示词注入防护：`A` = 无防护，`C` = 隔离+防护后缀 |
| `app.language` | 界面语言（zh_CN / ja_JP / en_US） |

> 修改 config.json 前建议先备份一份。改错可能导致程序无法启动（删掉 config.json 重启可重新进入配置向导）。

### 9.13 角色与会话导入导出

**角色导出/导入**：角色文件夹本身可直接复制、打包 zip 分享。把角色文件夹放进 `characters/` 即可被识别。程序支持从 zip 或文件夹导入。

**会话导出/导入**：见 [9.2 会话管理](#92-会话管理)。导出的 zip 内含 `messages.json` 和音频，可完整迁移到另一台电脑。

---

## 十、常见问题 FAQ

### Q1：双击 go-llm-tts.bat 后闪一下就没了？

- 说明依赖未安装。先双击 `install_deps.bat` 完成安装
- 或在命令行手动运行 `venv\Scripts\python.exe app.py` 查看报错信息

### Q2：状态栏显示"🔴 TTS API 离线"？

- GPT-SoVITS 服务没启动。回到[第 3.4 步](#34-启动-gpt-sovits-语音服务)启动
- 检查 GPT-SoVITS 路径和 TTS API 地址是否配置正确（`http://127.0.0.1:9880`）

### Q3：发送消息后提示"LLM 调用失败"？

- 检查 API Key 是否正确、是否过期
- **粘贴 API Key 时不要带入多余空格/换行**（复制到邮箱/编辑器再粘贴容易出现，会导致请求被拒，表现为"连接错误"）
- 检查 Base URL / 模型名是否与提供商匹配（第三方中转可在提供商网站查询可用模型列表）
- 检查网络连接（某些网络需要配置代理）

### Q4：AI 有文字回复但没有声音？

- 确认 TTS API 在线
- 检查角色是否配置了参考音频（音色样本）
- 语音回复在回复下方，可能需要点击播放

### Q5：语音合成很慢或卡住？

- GPT-SoVITS 首次使用需要加载模型，耐心等待
- 电脑没有独显（GPU）时合成较慢，属正常
- 超长回复会自动分片，可等待完成

### Q6：如何让 AI 用特定语言回复？

右栏"回复文字语种"选择目标语言；"合成语音语种"选择语音的语言模式。

### 附：错误码与运行报告（v1.2.0）

- **错误码**：WebUI 报错会带错误码，形如 `[LLM-004] 没有可用的 LLM 提供商`、`[TTS-003] TTS 合成失败`。常见前缀：`LLM-`（大模型）、`TTS-`（语音）、`CFG-`（配置）、`STP-`（一键启动）、`UI-`（输入/操作）、`SYS-`（系统）
- **一键启动报告**：`logs\startup_report_<日期>.txt`（及 `.jsonl`）记录每次一键启动的步骤与 STP 错误码（如探测 GPT-SoVITS 失败 `STP-001`、TTS 启动超时 `STP-004`）
- **发送消息报告**：`logs\run_report_<日期>.txt`（及 `.jsonl`）记录每次发送消息的全流程步骤与耗时（输入校验→角色→会话→记忆→上下文→Lorebook→LLM→TTS→保存），失败步骤带错误码
- 报告按天命名、自动保留 7 天；JSON 版（`.jsonl`）每行一条 JSON，便于程序读取

### Q7：角色列表是空的？- 检查 `characters/` 下是否有角色文件夹（每个角色需含 `character.json`）
- 点击"刷新角色"按钮
- 或通过"编辑角色"手动创建一个

### Q8：如何备份我的数据？

- 复制 `characters/`、`conversations/`、`config.json` 三个位置即可
- 或使用会话导出功能导出单个会话

### Q9：如何更新项目？

- 用 Git 克隆的：在项目文件夹打开命令行，输入 `git pull`
- 下载 zip 的：重新下载解压，把旧 `characters/`、`conversations/`、`config.json` 复制到新文件夹

### Q10：端口被占用（提示无法找到可用端口）？

程序会自动尝试 7861→7862→… 递增寻找可用端口，一般无需处理。也可在 config.json 中修改 `app.port`。

---

## 十一、术语表

| 术语 | 解释 |
|------|------|
| **API** | 程序间通信的接口约定 |
| **API Key** | 使用付费 AI 服务的凭证（密钥） |
| **LLM** | 大语言模型，AI 的"大脑" |
| **TTS** | 文字转语音 |
| **GPT-SoVITS** | 本项目使用的语音合成引擎 |
| **Gradio** | 本项目使用的网页界面框架 |
| **OpenAI 兼容 API** | 一种通用的 AI 接口标准，DeepSeek/通义等都兼容 |
| **克隆 (clone)** | 用 Git 把远程仓库完整复制到本地 |
| **依赖** | 项目运行所需的第三方库 |
| **虚拟环境 (venv)** | 独立隔离的 Python 运行环境 |
| **config.json** | 本项目配置文件 |
| **Lorebook** | 角色的世界观知识库 |
| **Markdown** | 一种排版标记语言（#标题、**加粗**等） |
| **端口** | 程序对外服务的"门牌号"，如 7861、9880 |
| **7-Zip / WinRAR** | 解压缩软件 |
| **路径** | 文件在电脑中的"地址" |

---

## 十二、安全注意事项

1. **API Key 是敏感信息**：只填写在本机配置中，绝不分享给他人，不截图发送
2. **config.json 含密钥**：备份/分享数据时**务必先删除 config.json** 或其中的 api_key 字段
3. **本程序无登录密码**：默认开放局域网访问，请勿在公共网络长期运行
4. **警惕提示词注入**：不要在对话中让 AI 泄露系统提示词；必要时开启 `prompt_protection.mode = "C"`
5. **数据安全**：角色、会话数据都保存在本机，定期备份
6. **不要在不可信渠道下载 GPT-SoVITS 压缩包**：认准官方项目来源，防止捆绑恶意软件

---

## 附：一图看懂运行流程

```
你打字："学姐，今天天气怎么样？"
        │
        ▼
┌─────────────────────┐
│  本程序（Gradio）    │
│  1. 保存你的消息     │
│  2. 取角色人设+知识  │
│  3. 查 Lorebook     │
└────────┬────────────┘
         │ 发送请求
         ▼
┌─────────────────────┐
│  LLM（大脑）         │
│  用"学姐"人设思考回复 │
└────────┬────────────┘
         │ 返回文字："今天阳光很好呢~"
         ▼
┌─────────────────────┐
│  本程序              │
│  4. 保存 AI 回复     │
│  5. 交给 GPT-SoVITS │
└────────┬────────────┘
         │
         ▼
┌─────────────────────┐
│  GPT-SoVITS（声优）  │
│  用学姐音色合成语音   │
└────────┬────────────┘
         │
         ▼
你看到文字 + 听到语音 ✅
```

---

## 附：v1.1.x 新功能速览

- **会话回收站**：误删的会话不会消失，在侧栏「工具-回收站」可恢复或清空；满 30 天会提醒清理
- **角色卡导入**：在侧栏「角色」里点「导入角色卡」，选择 TavernAI 的 PNG 卡或 RisuAI/Chub 的 JSON 卡，程序自动识别格式并导入（含头像），导入后可继续编辑
- **侧栏可调整**：把鼠标移到侧栏与聊天区之间的竖条上，按住左右拖动即可调整侧栏宽度（200~600px，记住你的设置）；点顶部 `☰ 侧栏` 按钮一键折叠/展开
- **移动端**：手机/窄屏下侧栏与聊天区自动上下堆叠，拖动分隔条隐藏；点 `☰ 侧栏` 仍可折叠/展开
- **长期记忆**：默认开启，程序会记住你聊过的偏好/事实（如"我喜欢弹钢琴"），同一角色下次对话还记得（可在「高级设置-长期记忆」关掉或清空）
- **高级设置**：侧栏「高级设置」可调性能（设备/并发）、会话闲置超时、通知音效；需要科学上网时填「代理」即可让 LLM/TTS 也走代理
- **会话级提供商**：在「配置」里可为某个会话单独指定 LLM 提供商（如"这个会话用通义"），其他会话仍跟随全局
- **API Key 安全**：设置页不再回显已保存的密钥，留空表示"保持不变"

---

*本指南基于项目 v1.3.4 编写。祝使用愉快！*
